


/**
 *
 * @author Chotek
 * @author JGorny
 *
 * Data Encryption Standard (DES) is a block cipher algorithm that takes plain
 * text in blocks of 64 bits and converts them to ciphertext using keys of 48 bits.
 * It is a symmetric key algorithm, which means that the same key is used for encrypting and decrypting data.
 */

public class DES extends Algorithm {

    public DES() {
        super("DES", true);
    }
}
